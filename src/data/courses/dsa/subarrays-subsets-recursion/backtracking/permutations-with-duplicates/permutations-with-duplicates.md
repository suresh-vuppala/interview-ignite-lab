Generate unique permutations with duplicate elements.

## Approach
- Sort array
- Skip duplicates at same level
- Use set or visited array

## Complexity
- Time: O(n!)
- Space: O(n)

## Code
```code```