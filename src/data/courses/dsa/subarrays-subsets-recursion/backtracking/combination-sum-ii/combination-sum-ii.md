Find unique combinations (no reuse, duplicates in input).

## Approach
- Sort array
- Skip duplicates at same level
- Each element used once

## Complexity
- Time: O(2^n)
- Space: O(n)

## Code
```code```