#include <vector>
#include <unordered_map>
using namespace std;

class Solution {
public:
    // ============ MEMOIZATION (TOP-DOWN) ============
    int robMemo(int nums) {
        if (len(nums) == 1) {
            return nums[0];
    }
    // ============ TABULATION (BOTTOM-UP) ============
    int rob(int nums) {
        if (len(nums) == 1) {
            return nums[0];
    }
};
