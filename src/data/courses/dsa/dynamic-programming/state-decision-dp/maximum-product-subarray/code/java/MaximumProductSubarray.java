class Solution{public int maxProduct(int[]nums){int maxP=nums[0],curMax=nums[0],curMin=nums[0];for(int i=1;i<nums.length;i++){if(nums[i]<0){int t=curMax;curMax=curMin;curMin=t;}curMax=Math.max(nums[i],curMax*nums[i]);curMin=Math.min(nums[i],curMin*nums[i]);maxP=Math.max(maxP,curMax);}return maxP;}}