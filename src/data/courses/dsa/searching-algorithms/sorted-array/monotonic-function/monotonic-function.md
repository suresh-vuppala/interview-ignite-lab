# Monotonic Function

A monotonic function is one that either never decreases or never increases. In the context of arrays and searching, we consider an array "monotonic" if its elements are sorted in increasing or decreasing order.

Understanding monotonicity allows the application of binary search and other techniques on custom conditions.